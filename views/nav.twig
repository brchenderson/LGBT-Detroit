
<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
<a href="#"  id="donate" class="btn btn-donate" data-toggle="modal" data-target="#donateModal">Support Us</a>
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
    <a href="{{site.url}}" class="navbar-brand"><img src="{{function('get_header_image') }}"/></a>
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navigation">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
              <div class="clearfix"></div>

    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="main-navigation">    
      <ul class="nav navbar-nav" >            
        {% for item in menu.get_items %}                
          <li><a href="{{item.permalink}}" class="navlink">{{item.title}}</a></li>                 
        {% endfor %}
        <li class="searchli">
              <form action="{{site.link}}" id="searchform" class="navbar-form" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search" name="s" id="s">
        </div>
        <button type="submit" value="" id="searchsubmit" class="fa fa-search submit"></button>
      </form>      
      </li>
      </ul>
    </div><!-- /.navbar-collapse -->
    
  </div><!-- /.container-fluid -->
</nav>